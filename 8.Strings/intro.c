// C does not have a String type to easily create string variables. Instead, you must use the char type 
// create an array of characters to make a string in C:
// you have to make array of strings

// char greetings[] = "Hello World!";
// printf("%s", greetings);

// Accesing a string
    // char greetings[] = "Hello World!";
    // printf("%c", greetings[0]); 
    // this will print "H"

// Modifing a string
    // char greetings[] = "Hello World!";
    // greetings[0] = 'J';
    // printf("%s", greetings);
    // // Outputs Jello World! instead of Hello World!

// Loop through a string
    //     int main() {
    //   char carName[] = "Volvo";
    //   int i;
    
    //   for (i = 0; i < 5; i++) {
    //     printf("%c\n", carName[i]);
    //   }

    //   return 0;
    // }